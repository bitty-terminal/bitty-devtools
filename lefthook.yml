# Enforced Git hooks. Tool invocations and version pins live in the justfile.
commit-msg:
  commands:
    commit-check:
      run: just commit-check {1}

# The pre-commit hook intentionally runs the same full quality gate as CI.
pre-commit:
  commands:
    check:
      run: just check
