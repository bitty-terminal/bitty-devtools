{
  "name": "bitty-devtools",
  "private": true,
  "version": "0.0.1",
  "description": "Human-facing diagnostics client for local debugging over Panel Runtime and compat matrix (phase 2, live IPC via Unix socket 0600 / Windows pipe with peer-creds, advanced tracing and control).",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test",
    "check:types": "tsc -p tsconfig.json --noEmit",
    "fmt:check": "prettier --check . --ignore-unknown",
    "lint": "markdownlint-cli2"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@types/bun": "1.4.0",
    "bun-types": "1.4.0",
    "typescript": "7.0.2"
  }
}
